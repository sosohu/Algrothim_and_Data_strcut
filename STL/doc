List Problmes:
o1. 求单链表中结点的个数
o2. 将单链表反转 // new 一个新的头节点，然后不断插入到头节点后面， 似乎可以不用new，另外一种方法?
o3. 查找单链表中的倒数第K个结点（k > 0）
o4. 查找单链表的中间结点
o5. 从尾到头打印单链表
o6. 已知两个单链表pHead1 和pHead2 各自有序，把它们合并成一个链表依然有序
o7. 判断一个单链表中是否有环
o8. 判断两个单链表是否相交
o9. 求两个单链表相交的第一个节点
o10. 已知一个单链表中存在环，求进入环中的第一个节点
o11. 给出一单链表头指针pHead和一节点指针pToBeDeleted，O(1)时间复杂度删除节点pToBeDeleted
o12.	在单链表的指定结点前插入一个结点


1. 构造链表，单向或者双向链表，一些基本的方法，　　双向链表查找的效率
2. 有环？　快慢指针，哈希，插桩, 环入口：　
3. 随机指针拷贝: O(n^2), 哈希对应，插桩法O(n)
4. 排序：　归并(递归，迭代)，快排
5. 链表相交,　哈希，最后一个节点, 有亮点
